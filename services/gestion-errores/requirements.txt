# Dependencias de ejecución. Versiones fijadas a mano: sin uv no hay lockfile,
# así que un rango abierto haría irreproducible la imagen.
# La librería compartida NO va aquí: se instala desde ruta en el Dockerfile.
flask==3.1.3
gunicorn==26.2.0
